{
  "name": "chinese-people-can-fly",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build --config vite.offline.config.ts && vite build",
    "build:offline": "tsc --noEmit && vite build --config vite.offline.config.ts",
    "preview": "vite preview",
    "verify:cannon-fuse": "node tools/verify_cannon_fuse_effect.mjs",
    "verify:impact": "node tools/verify_impact_effect.mjs",
    "verify:ufo": "node tools/verify_ufo_gameplay.mjs"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.2.1"
  }
}
